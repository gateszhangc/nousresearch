apiVersion: v1
kind: Service
metadata:
  name: nousresearch
  labels:
    app.kubernetes.io/name: nousresearch
    dokploy-fleet/project: nousresearch
spec:
  selector:
    app.kubernetes.io/name: nousresearch
  ports:
    - name: http
      port: 80
      targetPort: 3000
